package com.puresound.web;

import java.util.Arrays;
import java.util.Set;

public class QuickSort {
	public int[] qSort(int[] arrays) {
		if(arrays.length < 2) {
			return arrays;
		}else {
			final int pivot = arrays[0];
			int[] less;
			int[] remaining = Arrays.copyOfRange(arrays, 1, arrays.length);
			for(int i : remaining) {
				
			}
			
			Set<String> friends = Set.of("Raphael", "Olivia", "Thibaut");
			friends.add("abc");
								
			
		}
	}
}
